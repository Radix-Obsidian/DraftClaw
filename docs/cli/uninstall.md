---
summary: "CLI reference for `draftclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `draftclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
draftclaw uninstall
draftclaw uninstall --all --yes
draftclaw uninstall --dry-run
```
